;;; init-which-key.el --- `which-key` package setting.  -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package which-key
  :ensure t
  :config
  (which-key-mode))

(provide 'init-which-key)
;;; init-which-key.el ends here
